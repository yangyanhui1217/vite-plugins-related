{
  "name": "vite-plugin-transform__require-to-import",
  "version": "1.0.8",
  "description": "替换第三方组件里面__require方法",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild ./src/index.js --bundle --outdir=dist --format=cjs",
    "vite": "vite"
  },
  "author": "yangyanhui",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.38"
  },
  "keywords": [
    "vite-plugin",
    "vite",
    "esbuild"
  ],
  "devDependencies": {
    "vite": "^2.9.5",
    "vite-plugin-vue2": "^2.0.0"
  }
}
